{
  "name": "kusama-js",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build": "rimraf dist/ && babel ./ --out-dir dist/ --ignore ./node_modules,./.babelrc,./package.json,./npm-debug.log --copy-files",
    "standard_trade": "npm run build && node dist/standard_trade.js",
    "funds_back_to_seller": "npm run build && node dist/funds_back_to_seller.js",
    "funds_dispute_to_buyer": "npm run build && node dist/funds_dispute_to_buyer.js"
  },
  "license": "MIT",
  "dependencies": {
    "@polkadot/api": "^1.13.1"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-es2015": "^6.24.1",
    "rimraf": "^3.0.2"
  }
}
